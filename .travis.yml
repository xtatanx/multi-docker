sudo: required
services:
  - docker
before_install:
  - docker build -t jhonnatang/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true jhonnatang/react-test npm test

after_success:
  - docker build -t jhonnatang/multi-client ./client
  - docker build -t jhonnatang/multi-nginx ./nginx
  - docker build -t jhonnatang/multi-server ./server
  - docker build -t jhonnatang/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push jhonnatang/multi-client
  - docker push jhonnatang/multi-nginx
  - docker push jhonnatang/multi-server
  - docker push jhonnatang/multi-worker
